# ROADMAP

- M1: Anonymizer + stratified sampler (done).
- M2: Label generator + execution filter — initial 3k Q→SQL pairs.
- M3: LoRA train (8B), eval suite, model card, private image delivery.
- M4: 70B teacher run + distillation to 8B.
- M5: Site-local trainer CLI + federated PoC (adapter aggregation).
